<?php
    
    // $getData = file_get_contents('php://input');
    // $i = Json_decode($getData);
    // $date = date("d/m/y");
    // $time = date("H:i");
    // $data = $date.','.$time.','.$i->temp.','.$i->hum.','.$i->pm."\n";
    // $data = "t\n";
    // $file = fopen("{{ asset('data/data.txt') }}", "a") or die("Unable to open file!");
    // fwrite($file, $data);
    // fclose($file);
    // $file = ExamFile::where('data');
    // $content = fopen(Storage::path('data/data.txt'),'a');

?>